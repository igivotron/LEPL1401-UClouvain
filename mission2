a = int(input("Valeur de a : "))
b = int(input("Valeur de b : "))
c = int(input("Valeur de c : "))

test =100

for x in range(1, test):
    for y in range(1, test):
        for z in range(1, test):
            eq = pow(x,a) + pow(y,b)
            if eq == pow(z,c):
                print(x, y, z)
